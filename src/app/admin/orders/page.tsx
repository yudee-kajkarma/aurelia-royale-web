import { AdminPlaceholderPage } from "@/components/admin/AdminPlaceholderPage";

export default function AdminOrdersPage() {
  return (
    <AdminPlaceholderPage
      eyebrow="Admin Orders"
      title="Order Operations Workspace"
      description="Use this area for all-order review, status workflows, and downstream admin tooling when you are ready to add it."
    />
  );
}